# [TODO] 下载完成自动替换 — TODO

## 目标
实现"搜索→下载→整理→替换旧文件"的完整闭环，减少手动干预。

## 功能清单

### 1. 下载完成实时刷新
- [ ] 1.1 下载任务状态变为 completed 时，自动触发该文件夹的局部刷新（不全量扫描）
- [ ] 1.2 前端下载管理面板中，completed 任务旁显示"在媒体库中查看"按钮，点击跳转到对应文件夹
- [ ] 1.3 快速同步完成后，如果有新增文件，自动刷新前端卡片列表

### 2. 归位与替换（核心链路）✅
> 已由 `file_relocator.py` 实现，作为 V3 整理流水线的客户端。

- [x] 2.1 归位探测：`relocate()` — 对新下载资源执行标准化命名推演 + 冲突检测
- [x] 2.2 冲突检测：`_detect_conflicts_v2()` — 新旧文件对比（路径+文件名双层匹配）
- [x] 2.3 确认替换：`confirm_replace()` — 旧资源入回收站 → 新资源整理归档
- [x] 2.4 保留两者：`archive_both()` — 旧资源移到 [OLD] 子目录
- [x] 2.5 取消替换：`cancel_replace()` — 沙盒中的新文件入回收站
- [x] 2.6 安全预案：
  - 旧文件不直接删除，先移入回收站（复用 recycle_bin.py）
  - 白名单兜底：qB 没给白名单时通过磁盘扫描自动构建
  - 推演失败不动旧文件，报错提示

### 3. 自动整理开关（全自动模式）
- [ ] 3.1 下载管理面板顶部增加"下载完成自动整理"开关
- [ ] 3.2 开启后，任务 completed 时自动执行归位+替换流程
- [ ] 3.3 自动整理的操作全部记录到历史，支持回滚
- [ ] 3.4 配置项存入 AppConfig（auto_organize_on_complete: bool）

### 4. 标准化结构兼容新旧共存
> 散装封装和季化已由 organizer.py 的 `reorganize_seasons` 实现（扁平 tv 强制季化 + 散落视频按季号归入）。

- [x] 5.1 散装封装：tv 标签下散装视频由整理流水线处理
- [x] 5.4 标准化 vs 分类的职责边界已明确（classify → structure → replace）
- [ ] 5.2 新旧共存检测：同目录下新旧文件对比替换（部分由 file_relocator 覆盖，但仅限下载触发场景）
- [ ] 5.3 分辨不出新旧时的处理策略
- [ ] 5.5 标准化前 dry_run 预览

## 补充说明
- 快速同步的作用：发现新增/删除的文件，更新媒体库索引。不做整理和替换。
- 扫描的作用：全量 ffprobe 获取详细元数据。慢但信息全。
- 替换的核心原则：旧文件永远先进回收站，不直接删除。所有操作可回滚。
