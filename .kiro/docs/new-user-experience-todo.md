# [TODO] new-user-experience-todo.md

> 新用户体验优化：解决非创作者用户的引导、理解和使用障碍。

---

## 核心问题

当前系统是为作者自用设计的，默认假设：
- 用户有 NAS + SMB 共享
- 媒体库有"电影"/"电视剧"等一级分类文件夹
- 用户懂 Prowlarr/qBittorrent/TMDB API Key
- 用户知道什么是 NFO/刮削/影子名

新用户面对的实际情况：
- 可能只有一个平铺的视频文件夹
- 不知道系统能做什么
- 不知道从哪里开始
- 看到空白页面不知道下一步

---

## 已完成

- [x] **引导步骤 1 — 添加媒体目录**：空白状态下两个选项提示引导
- [x] **引导步骤 2 — 媒体库结构选择**：空白状态下两个选项提示引导
- [x] **媒体库结构灵活化**：去除一级分类文件夹硬性假设，支持平铺结构
- [x] **首页空状态引导**：无媒体库时显示引导卡片
- [x] **术语优化 — 影子名**：已改为更友好的表述
- [x] **术语优化 — 清洗名**：已改为"索引名称"

---

## 待办清单

### P0：首次启动引导（剩余）

- [x] **引导步骤 3 — 可选配置**：
  - TMDB API Key（提示获取方式 + 链接）+ 测试按钮
  - 代理配置（如果需要访问 TMDB）
  - 跳过按钮（"稍后配置"）
- [x] **引导完成**：添加路径后自动触发扫描；首次扫描完成后弹出配置引导

### P1：空状态引导（剩余）

- [x] **搜索空状态**：
  - 无搜索源时显示"未安装搜索源插件" + 引导去插件中心
- [x] **发现页空状态**：
  - 无 discover 插件时显示"安装发现推荐插件解锁热门推荐"
- [x] **订阅空状态**：
  - 无 RSS 源插件时显示"未安装订阅源插件" + 引导
  - 有插件但无订阅时显示"暂无订阅，在推荐或探索中点击订阅开始追更"
- [x] **下载管理空状态**：
  - 无下载器时显示"未安装下载器插件"（已有）

### P1：术语与文案优化（剩余）

- [x] **避免专业术语直接暴露**：
  - "刮削" → "识别"/"自动识别"（按钮、进度、状态提示）
  - "NFO" → 在 UI 中不直接展示（已是内部概念）
  - "Provider" → 用户看到的已是"数据源"/"搜索源"等友好名称
  - "Action Plan" → 用户看到的已是"整理方案"/"整理替换预览"
  - "dry-run" → 用户看到的已是"预览"
- [x] **状态提示人性化**：
  - "scrape_failed" title → "识别失败"
  - 整理步骤标签优化（旧刮削清理→旧数据清理，刮削确权→识别匹配）

### P1：设置页简化

- [x] **分层设置**：
  - 插件设置（TMDB Key、Prowlarr 地址等）在对应插件安装后才出现（已有）
  - AI 功能区域已折叠展开
  - 高级设置（播放器路径、缓存管理、标准化名称、回收站）折叠到"高级设置"区域
- [x] **配置验证与反馈**：
  - TMDB API Key 测试按钮（设置页 + SetupWizardModal）
  - Prowlarr 地址连通性测试按钮
  - qBittorrent 连通性测试按钮
  - OpenList 连通性测试按钮
  - 修改配置后自动重置测试状态

### P2：操作引导与提示

- [ ] **首次扫描完成后**：
  - 弹出摘要："发现 X 部电影、Y 部剧集、Z 个未识别"
  - 引导下一步："点击未识别的文件夹手动匹配"或"安装 TMDB 插件自动识别"
- [ ] **功能发现提示**：
  - 首次进入文件夹详情时，轻提示"点击封面可以更换海报"
  - 首次看到质量评分时，轻提示"红色表示低质量，可以搜索更好的版本"
  - 首次看到完整性条时，轻提示"灰色表示缺失的集数"
- [ ] **错误恢复引导**：
  - 网络错误时提示"检查代理配置"而非只显示错误码
  - TMDB 限频时提示"请稍后重试"而非静默失败

### P2：多语言/国际化预留

- [ ] **UI 文案集中管理**：
  - 所有用户可见文案提取到 `i18n/zh-CN.json`
  - 当前只做中文，但结构预留多语言切换
- [ ] **日期/数字格式**：
  - 文件大小、日期显示跟随系统 locale

### P3：响应式与移动端适配

> ⚠️ 执行前必须先 git commit 当前状态作为备份，防止布局/UI 被改坏。

- [ ] **侧边栏折叠**：小屏幕自动折叠，汉堡菜单展开
- [ ] **卡片网格自适应**：根据屏幕宽度调整列数
- [ ] **触摸友好**：按钮最小 44px 触摸区域
- [ ] **PWA 基础**：manifest.json + service worker（离线缓存静态资源）

---

## 执行优先级

```
P0（必须在首次公开发布前完成）：
  - 首次启动引导剩余步骤（可选配置 + 首次扫描触发）

P1（首次发布后快速迭代）：
  - 空状态引导（搜索/发现/订阅/下载）
  - 术语文案优化（剩余项）
  - 设置页简化

P2（用户反馈驱动）：
  - 操作引导与提示
  - 国际化预留

P3（长期演进，需先备份）：
  - 响应式适配
  - PWA
```

---

## 与插件中心的关系

- 插件中心是 P0 的一部分（`plugin-center-todo.md`）
- 空状态引导依赖插件中心（"请安装 XX 插件"的引导指向插件中心）
- 设置页简化依赖插件中心（插件配置只在安装后出现）
- 两个 TODO 应该并行推进，插件中心先行（提供基础设施），新用户体验跟进（利用基础设施做引导）

---

## 已移除项（不做/不适用）

- ~~操作按钮文案清晰化~~：原有文案更贴切，不改
- ~~手动分类入口~~：已随媒体库结构灵活化一并解决
