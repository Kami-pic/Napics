# [TODO] 下一步工作清单

---

## 订阅系统（Phase 0-3b 已完成）

### 紧急

- [ ] 发现页详情匹配错误：匹兹堡医护前线显示成年轻的皮特先生、无敌少侠显示成同名电影、黑袍纠察队显示成捉鬼小精灵。不是订阅改动引起的，需要单独排查 TMDB 搜索匹配逻辑
- [ ] 订阅 tab 点搜索时，应该只搜用户在订阅配置中选择的源，而不是全部源。比如用户只选了 mikan，搜索结果不应该出现 Prowlarr 的内容。目前搜索弹窗不支持按源过滤，需要给 SearchModal 加 sources 参数
- [ ] 订阅卡片上应该显示"正在用哪些源跟踪"，比如"mikan + eztv"。旧订阅因为没选过源所以显示为空，新订阅选了源后会正常显示

### 重要

- [ ] 保存路径应该根据内容类型自动填入媒体库对应的分类路径（电影→电影目录，电视剧→电视剧目录），洗版时应该用本地文件所在的目录
- [ ] 日漫的播出日历 TMDB 经常没数据，需要接入 Bangumi Calendar API 作为备选
- [ ] 同一部作品已经有追更订阅时，再点洗版应该提示用户"已有追更，是否同时开启洗版"
- [ ] RSS 匹配器需要增强跨语言匹配能力，比如"葬送的芙莉莲"要能匹配到 "Sousou no Frieren"

### 延后

- [ ] 搜索结果缓存（同一个源+同一个关键词 30 分钟内不重复请求）
- [ ] 全局速率限制（避免 20 个订阅同时搜索把源站搞限频）
- [ ] 扩展更多 RSS 源：动漫花园、ACG.RIP、Bangumi Moe、YTS
- [ ] 反馈闭环：搜索日志、下载完成通知、推送接口

---

## 技术债务

- [x] `pan_models.py` Pydantic V2 迁移
- [ ] `routes/search.py` 剩余端点清理（核心逻辑已抽到 search_service.py）
- [x] `routes/discover.py` 拆分 → discover_enrich.py
- [x] 后端 `print()` → `logging` 模块
