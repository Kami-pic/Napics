---
inclusion: always
---

每次新对话开始时，必须先读取 .kiro/PROJECT_MEMORY.md 获取完整项目记忆。
这是唯一的项目记忆源，不要在其他地方维护副本。


⚠️ AI 工作流程（必须遵守）
所有回复、思考过程、代码注释全部用中文 — 不允许英文回复
跑脚本/全量操作必须等用户明确确认 — 运行脚本、全量数据操作等必须等用户说"确认/开始/跑吧"
先搜现有代码，不重复创建文件/脚本
优化而不是重写 — 在现有文件上改，不要新建脚本（除非一次性任务）
不要写 spec — 用 TODO 清单推进
批量操作先测 5 个样本，用户确认后才全量运行
不要一次读多个大文件，按需读取
每天第一次对话开始时，主动总结前一天完成的工作内容（精炼、非技术向、逐条列出）
改完代码后自己跑构建验证（npx next build），确认无编译错误再告诉用户完成
用户说"没变化"意味着显示/渲染有问题，不是保存失败，不要建议刷新缓存（用户每次都强制刷新）
后台数据和角色快照是独立路径 — 改一个不要影响另一个
总结工作时查 git log，不要只看当前对话，要追溯完整历史